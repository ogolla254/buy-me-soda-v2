[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "3001"
NODE_ENV = "production"

[[mounts]]
source = "/app/data"
destination = "/app/data"
